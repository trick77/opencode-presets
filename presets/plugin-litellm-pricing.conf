// @name: plugin-litellm-pricing
// @description: Adds the opencode-plugin-litellm-pricing plugin: discovers a LiteLLM
//   proxy's models at runtime and adds them to the picker with real per-model
//   pricing from a LiteLLM-format model catalog instead of $0. The catalog URL has no
//   default — install provider-litellm too, or the models arrive unpriced.
// @author: Jan <jan@trick77.com>
// @version: 0.9.1
// @path: plugin
// @mode: append
// @pins: opencode-plugin-litellm-pricing 0.8.1

["opencode-plugin-litellm-pricing@0.8.1"]
