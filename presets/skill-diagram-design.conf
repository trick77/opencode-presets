// @name: skill-diagram-design
// @description: Needs a clone of github.com/cathrynlavery/diagram-design on disk
//   first — clone it yourself, then answer with the absolute path to its skills/
//   dir; the install refuses if it is not there. Registers the diagram-design
//   skill: editorial diagram types rendered as self-contained HTML + SVG. No tags
//   upstream, so you track main: update with git pull, and review what you pull.
//   Fetches and runs nothing. Uninstall by deleting the skills.paths entry.
// @author: Jan <jan@trick77.com>
// @version: 0.3.1
// @path: skills.paths
// @mode: append
// @prompt: skillsDir | dir | absolute path to your diagram-design clone's skills/ dir | | clone it first: git clone https://github.com/cathrynlavery/diagram-design, then answer with the skills/ dir inside that clone
[
  "{{prompt:skillsDir}}"
]
