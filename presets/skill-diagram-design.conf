// @name: skill-diagram-design
// @description: Registers the diagram-design skill from
//   github.com/cathrynlavery/diagram-design — editorial diagram types rendered as
//   self-contained HTML + SVG. Clone it yourself, then give the absolute path to the
//   skills/ dir inside that clone; the install refuses if it is not there. No tags
//   upstream, so you track main: update with git pull, and review what you pull.
//   Fetches and runs nothing. Uninstall by deleting the skills.paths entry by hand.
// @author: Jan <jan@trick77.com>
// @version: 0.2.0
// @path: skills.paths
// @mode: append
// @prompt: skillsDir | dir | absolute path to your diagram-design clone's skills/ dir
[
  "{{prompt:skillsDir}}"
]
