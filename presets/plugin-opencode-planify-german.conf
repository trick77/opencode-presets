// @name: plugin-opencode-planify-german
// @description: Needs instructions-opencode-planify-german too, or nothing tells
//   the agent to use the tool — install the opencode-planify-german bundle for
//   both. Adds the opencode-planify-german plugin from npm, pinned to 0.3.2, which
//   registers `plan_render`: takes a plan as JSON, validates it against a schema,
//   renders a self-contained HTML file to docs/plans/<TICKET>-<slug>.html and opens
//   it. Restart opencode afterwards — plugins resolve at start.
// @author: Jan <jan@trick77.com>
// @version: 0.3.2
// @path: plugin
// @mode: append
// @pins: opencode-planify-german 0.3.2

["opencode-planify-german@0.3.2"]
