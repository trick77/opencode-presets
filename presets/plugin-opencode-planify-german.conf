// @name: plugin-opencode-planify-german
// @description: Adds the opencode-planify-german plugin, which registers the
//   `plan_render` tool: it takes a plan as JSON, validates it against a schema,
//   renders a self-contained HTML file to docs/plans/<TICKET>-<slug>.html and opens it
//   in the system's default browser. Pinned to tag v0.2.1. Without
//   instructions-opencode-planify-german nothing tells the agent to use it — install
//   the opencode-planify-german bundle to get both.
// @author: Jan <jan@trick77.com>
// @version: 0.2.1
// @path: plugin
// @mode: append
// @pins: opencode-planify-german 0.2.1

["opencode-planify-german@git+https://github.com/trick77/opencode-planify-german.git#v0.2.1"]
