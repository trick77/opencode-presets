// @name: plugin-opencode-planify-german
// @description: Adds the opencode-planify-german plugin, which registers the
//   `plan_render` tool: it takes a plan as JSON, validates it against a schema,
//   renders a self-contained HTML file to docs/plans/<TICKET>-<slug>.html and opens it
//   in the system's default browser. Pinned to 0.3.2 on npm, not a git spec: no git
//   binary needed. opencode resolves it on its next start, so restart it. Install the
//   opencode-planify-german bundle to get the rules too.
// @author: Jan <jan@trick77.com>
// @version: 0.3.2
// @path: plugin
// @mode: append
// @pins: opencode-planify-german 0.3.2

["opencode-planify-german@0.3.2"]
