// @name: plugin-planify
// @description: Adds the planify plugin from github.com/trick77/planify, which
//   registers the `plan_render` tool. The tool takes a plan as JSON, validates it
//   against a schema, renders a self-contained HTML file to
//   docs/plans/<TICKET>-<slug>.html and opens it in the system's default browser.
//   Pinned to tag v0.1.0. Without instructions-planify the agent is not told to use
//   it — install the planify bundle to get both.
// @author: Jan <jan@trick77.com>
// @version: 0.1.0
// @path: plugin
// @mode: append
// @pins: planify 0.1.0

["planify@git+https://github.com/trick77/planify.git#v0.1.0"]
