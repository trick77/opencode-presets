// @name: permissions-recommended
// @description: Bundle: an opinionated baseline. The read-only presets (shell,
//   git, toolchain probes, docker/podman), then the two deny presets, in that
//   order so the denies win. NOT a safety guarantee — it cuts prompting for
//   everyday commands and blocks a list of known footguns, nothing more.
//   Excludes permissions-build-tools, which lets the agent run project code
//   that no deny rule can see; add it and permissions-cluster-info yourself.
// @author: Jan <jan@trick77.com>
// @version: 0.2.0
// @include: permissions-shell-safe
// @include: permissions-git-safe
// @include: permissions-toolchain-info
// @include: permissions-container-info
// @include: permissions-deny-destructive
// @include: permissions-deny-cluster-write
