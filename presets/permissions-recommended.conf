// @name: permissions-recommended
// @description: Bundle: installs the read-only permission modules (shell, git,
//   toolchain probes, docker/podman) followed by the two deny modules, in that
//   order so the denies win. Deliberately excludes permissions-build-tools —
//   allowing npm/python/make lets the agent run project code, and `python -c`
//   executes commands opencode never sees, which the deny rules cannot catch.
//   Add that, and permissions-cluster-info, explicitly if you want them.
// @author: Jan <jan@trick77.com>
// @version: 0.1.0
// @include: permissions-shell-safe
// @include: permissions-git-safe
// @include: permissions-toolchain-info
// @include: permissions-container-info
// @include: permissions-deny-destructive
// @include: permissions-deny-cluster-write
