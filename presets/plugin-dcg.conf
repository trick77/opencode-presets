// @name: plugin-dcg
// @description: Adds the opencode-plugin-dcg plugin: every bash command is checked
//   by the external dcg (destructive command guard) binary before it runs, and the
//   destructive ones are blocked. Requires dcg on PATH — without it the plugin warns
//   once and commands run unchecked.
// @author: Jan <jan@trick77.com>
// @version: 0.1.0
// @path: plugin
// @mode: append
// @pins: opencode-plugin-dcg 0.1.0

["opencode-plugin-dcg@0.1.0"]
