// @name: opencode-planify-german
// @description: Bundle: the whole planify setup — the plugin providing the
//   `plan_render` tool, the German rules file that makes every plan go through it,
//   and the skill documenting the plan schema. Install all three or the setup is
//   inert: the rules alone name a tool that does not exist, the plugin alone is never
//   called. Replaces instructions-swiss-rules, which is gone: its language rules live
//   in the planify rules file, its hand-written HTML plans do not.
// @author: Jan <jan@trick77.com>
// @version: 0.3.2
// @include: plugin-opencode-planify-german
// @include: instructions-opencode-planify-german
// @include: skill-opencode-planify-german
